/target
debug.log
.claude
*.log
# Docker integration test SSH keys (private key must never be committed)
tests/docker/test_key
tests/docker/test_key.pub
# Local-only files (cross-compile config; audit notes and plans live in ignored/)
Cross.toml
/ignored/
